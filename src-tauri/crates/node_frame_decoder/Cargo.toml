[package]
name = "node_frame_decoder"
description = "VOFA-NEXT 帧解码状态机 — 镜像前端 DecoderBlock 块列表, 字节流 → 帧解析 → 输出端口值"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
schema_types = { workspace = true }
log = { workspace = true }