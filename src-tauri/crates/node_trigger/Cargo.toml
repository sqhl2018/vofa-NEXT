[package]
name = "node_trigger"
description = "VOFA-NEXT 触发器匹配器 — TriggerMatcher 规则引擎 (Exact/Prefix/Contains/Regex/Range/Glob)"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
regex = { workspace = true }
glob = { workspace = true }
log = { workspace = true }